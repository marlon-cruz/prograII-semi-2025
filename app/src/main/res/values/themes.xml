<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.PetCare20" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="BottomNavigation">
        <item name="android:background">@color/color_menu</item>
        <item name="itemIconTint">@color/color_letras</item>
        <item name="itemTextColor">@color/color_letras</item>
    </style>

    <style name="Theme.PetCare20" parent="Base.Theme.PetCare20" />
</resources>